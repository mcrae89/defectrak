import { isUndefined, getValue, isNullOrUndefined, setValue, uniqueID, isBlazor } from '@syncfusion/ej2-base/src/util';import { ModuleLoader, ModuleDeclaration } from '@syncfusion/ej2-base/src/module-loader';import { Base } from '@syncfusion/ej2-base/src/base';import { Observer, BoundOptions } from '@syncfusion/ej2-base/src/observer';import { ChildProperty } from '@syncfusion/ej2-base/src/child-property';import { Property, NotifyPropertyChanges } from '@syncfusion/ej2-base/src/notify-property-change';import { onIntlChange, rightToLeft, defaultCulture } from '@syncfusion/ej2-base/src/internationalization';import { createElement, addClass, removeClass, ElementProperties, select } from '@syncfusion/ej2-base/src/dom';import { validateLicense, createLicenseOverlay, componentList } from '@syncfusion/ej2-base/src/validate-lic';

/**
 * Interface for a class Component
 */
export interface ComponentModel {

    /**
     * Enable or disable persisting component's state between page reloads.
     *
     * @default false
     */
    enablePersistence?: boolean;

    /**
     * Enable or disable rendering component in right to left direction.
     *
     * @default false
     */
    enableRtl?: boolean;

    /**
     * Overrides the global culture and localization value for this component. Default global culture is 'en-US'.
     *
     * @default ''
     */
    locale?: string;

}