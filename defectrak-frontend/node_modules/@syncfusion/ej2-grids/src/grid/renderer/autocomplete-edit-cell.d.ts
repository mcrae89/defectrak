import { IEditCell } from '@syncfusion/ej2-grids/src/grid/base/interface';
import { Column } from '@syncfusion/ej2-grids/src/grid/models/column';
import { EditCellBase } from '@syncfusion/ej2-grids/src/grid/renderer/edit-cell-base';
/**
 * `AutoCompleteEditCell` is used to handle autocomplete cell type editing.
 *
 * @hidden
 */
export declare class AutoCompleteEditCell extends EditCellBase implements IEditCell {
    private object;
    private column;
    write(args: {
        rowData: Object;
        element: Element;
        column: Column;
        rowElement: HTMLElement;
        requestType: string;
    }): void;
    private selectedValues;
}
