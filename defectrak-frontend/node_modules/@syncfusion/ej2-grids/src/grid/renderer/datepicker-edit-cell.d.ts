import { Column } from '@syncfusion/ej2-grids/src/grid/models/column';
import { IEditCell, IGrid } from '@syncfusion/ej2-grids/src/grid/base/interface';
import { EditCellBase } from '@syncfusion/ej2-grids/src/grid/renderer/edit-cell-base';
import { Edit } from '@syncfusion/ej2-grids/src/grid/actions/edit';
/**
 * `DatePickerEditCell` is used to handle datepicker cell type editing.
 *
 * @hidden
 */
export declare class DatePickerEditCell extends EditCellBase implements IEditCell {
    edit: Edit;
    isMaskModuleInjected: boolean;
    constructor(parent?: IGrid);
    write(args: {
        rowData: Object;
        element: Element;
        column: Column;
        type: string;
        row: HTMLElement;
        requestType: string;
    }): void;
}
