import { IGrid, IFilterMUI } from '@syncfusion/ej2-grids/src/grid/base/interface';
import { Column } from '@syncfusion/ej2-grids/src/grid/models/column';
import { FilterSettings } from '@syncfusion/ej2-grids/src/grid/base/grid';
import { L10n } from '@syncfusion/ej2-base';
import { ServiceLocator } from '@syncfusion/ej2-grids/src/grid/services/service-locator';
import { Filter } from '@syncfusion/ej2-grids/src/grid/actions/filter';
import { FlMenuOptrUI } from '@syncfusion/ej2-grids/src/grid/renderer/filter-menu-operator';
import { Dialog } from '@syncfusion/ej2-popups';
export declare class BooleanFilterUI implements IFilterMUI {
    private parent;
    protected serviceLocator: ServiceLocator;
    private elem;
    private multiSelectElement;
    private value;
    private filterSettings;
    private dropInstance;
    private multiSelectCheckBoxInstance;
    private dialogObj;
    private dropdownOpen;
    private dropdownComplete;
    private multiSelectDropdownOpen;
    private multiSelectDropdownComplete;
    constructor(parent?: IGrid, serviceLocator?: ServiceLocator, filterSettings?: FilterSettings);
    create(args: {
        column: Column;
        target: HTMLElement;
        getOptrInstance: FlMenuOptrUI;
        localizeText: L10n;
        dialogObj: Dialog;
    }): void;
    write(args: {
        column: Column;
        target: Element;
        parent: IGrid;
        filteredValue: number | string | Date | boolean;
    }): void;
    read(element: Element, column: Column, filterOptr: string, filterObj: Filter): void;
    private createDropDownList;
    private createMultiSelectDropDown;
    private getBooleanInstance;
    private getMultiSelectInstance;
    private openPopup;
    private actionComplete;
    destroy(): void;
}
