import { IGrid } from '@syncfusion/ej2-grids/src/grid/base/interface';
import { EditRender } from '@syncfusion/ej2-grids/src/grid/renderer/edit-renderer';
import { ServiceLocator } from '@syncfusion/ej2-grids/src/grid/services/service-locator';
import { NormalEdit } from '@syncfusion/ej2-grids/src/grid/actions/normal-edit';
/**
 * `DialogEdit` module is used to handle dialog editing actions.
 *
 * @hidden
 */
export declare class DialogEdit extends NormalEdit {
    protected parent: IGrid;
    protected serviceLocator: ServiceLocator;
    protected renderer: EditRender;
    constructor(parent?: IGrid, serviceLocator?: ServiceLocator, renderer?: EditRender);
    closeEdit(): void;
    addRecord(data?: Object, index?: number): void;
    endEdit(): void;
    updateRow(index: number, data?: Object): void;
    deleteRecord(fieldname?: string, data?: Object): void;
    protected startEdit(tr?: Element): void;
}
