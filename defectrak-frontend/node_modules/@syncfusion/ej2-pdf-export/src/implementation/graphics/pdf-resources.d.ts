/**
 * PdfResources.ts class for EJ2-PDF
 */
import { PdfDictionary } from '@syncfusion/ej2-pdf-export/src/implementation/primitives/pdf-dictionary';
import { TemporaryDictionary } from '@syncfusion/ej2-pdf-export/src/implementation/collections/object-object-pair/dictionary';
import { PdfName } from '@syncfusion/ej2-pdf-export/src/implementation/primitives/pdf-name';
import { IPdfPrimitive } from '@syncfusion/ej2-pdf-export/src/interfaces/i-pdf-primitives';
import { IPdfWrapper } from '@syncfusion/ej2-pdf-export/src/interfaces/i-pdf-wrapper';
import { PdfDocument } from '@syncfusion/ej2-pdf-export/src/implementation/document/pdf-document';
import { PdfFont } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/fonts/pdf-font';
import { PdfTemplate } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/figures/pdf-template';
import { PdfBrush } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/brushes/pdf-brush';
import { PdfTransparency } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/pdf-transparency';
import { PdfBitmap } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/images/pdf-bitmap';
import { PdfImage } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/images/pdf-image';
/**
 * `PdfResources` class used to set resource contents like font, image.
 * @private
 */
export declare class PdfResources extends PdfDictionary {
    /**
     * Dictionary for the `objects names`.
     * @private
     */
    private pdfNames;
    /**
     * Dictionary for the `properties names`.
     * @private
     */
    private properties;
    /**
     * `Font name`.
     * @private
     */
    private fontName;
    /**
     * Stores instance of `parent document`.
     * @private
     */
    private pdfDocument;
    /**
     * Initializes a new instance of the `PdfResources` class.
     * @private
     */
    constructor();
    /**
     * Initializes a new instance of the `PdfResources` class.
     * @private
     */
    constructor(baseDictionary: PdfDictionary);
    /**
     * Gets the `font names`.
     * @private
     */
    private readonly names;
    /**
     * Get or set the `page document`.
     * @private
     */
    document: PdfDocument;
    /**
     * `Generates name` for the object and adds to the resource if the object is new.
     * @private
     */
    getName(obj: IPdfWrapper): PdfName;
    /**
     * Gets `resource names` to font dictionaries.
     * @private
     */
    getNames(): TemporaryDictionary<IPdfPrimitive, PdfName>;
    /**
     * Add `RequireProcedureSet` into procset array.
     * @private
     */
    requireProcedureSet(procedureSetName: string): void;
    /**
     * `Remove font` from array.
     * @private
     */
    removeFont(name: string): void;
    /**
     * Generates `Unique string name`.
     * @private
     */
    private generateName;
    /**
     * `Adds object` to the resources.
     * @private
     */
    add(font: PdfFont, name: PdfName): void;
    /**
     * `Adds object` to the resources.
     * @private
     */
    add(template: PdfTemplate, name: PdfName): void;
    /**
     * `Adds object` to the resources.
     * @private
     */
    add(brush: PdfBrush, name: PdfName): void;
    /**
     * `Adds object` to the resources.
     * @private
     */
    add(transparency: PdfTransparency, name: PdfName): void;
    /**
     * `Adds object` to the resources.
     * @private
     */
    add(image: PdfImage | PdfBitmap, name: PdfName): void;
}
/**
 * Used to create new guid for resources.
 * @private
 */
export declare class Guid {
    /**
     * Generate `new GUID`.
     * @private
     */
    static getNewGuidString(): string;
}
