/**
 * PdfAutomaticField.ts class for EJ2-PDF
 */
import { RectangleF, SizeF, PointF } from '@syncfusion/ej2-pdf-export/src/implementation/drawing/pdf-drawing';
import { PdfFont } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/fonts/pdf-font';
import { PdfBrush } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/brushes/pdf-brush';
import { PdfStringFormat } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/fonts/pdf-string-format';
import { PdfPen } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/pdf-pen';
import { PdfGraphics } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/pdf-graphics';
import { PdfGraphicsElement } from '@syncfusion/ej2-pdf-export/src/implementation/graphics/figures/base/graphics-element';
import { PdfPage } from '@syncfusion/ej2-pdf-export/src/implementation/pages/pdf-page';
/**
 * Represents a fields which is calculated before the document saves.
 */
export declare abstract class PdfAutomaticField extends PdfGraphicsElement {
    private internalBounds;
    private internalFont;
    private internalBrush;
    private internalPen;
    private internalStringFormat;
    private internalTemplateSize;
    protected constructor();
    bounds: RectangleF;
    size: SizeF;
    location: PointF;
    font: PdfFont;
    brush: PdfBrush;
    pen: PdfPen;
    stringFormat: PdfStringFormat;
    abstract getValue(graphics: PdfGraphics): string;
    abstract performDraw(graphics: PdfGraphics, location: PointF, scalingX: number, scalingY: number): void;
    performDrawHelper(graphics: PdfGraphics, location: PointF, scalingX: number, scalingY: number): void;
    draw(graphics: PdfGraphics): void;
    draw(graphics: PdfGraphics, location: PointF): void;
    draw(graphics: PdfGraphics, x: number, y: number): void;
    protected getSize(): SizeF;
    protected drawInternal(graphics: PdfGraphics): void;
    protected getBrush(): PdfBrush;
    protected getFont(): PdfFont;
    getPageFromGraphics(graphics: PdfGraphics): PdfPage;
}
